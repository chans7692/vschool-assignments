import React from 'react';
import {withGlobal} from './GlobalProvider';

const Playlist = () => {
        return (
            <div>
               
            </div>
        );
}

export default withGlobal(Playlist);